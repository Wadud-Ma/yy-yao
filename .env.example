# ================================================
# 易经卜卦服务 - 环境变量配置示例
# ================================================
# 复制此文件为 .env 并填写实际的配置值
# 注意: .env 文件不应提交到版本控制系统

# ================================================
# 数据库配置
# ================================================
DATABASE_URL=jdbc:mysql://localhost:3306/yy-yao?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=your_password_here

# ================================================
# JWT 认证配置
# ================================================
# 密钥长度必须至少32个字符,建议使用随机生成的字符串
# 生成方式: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-at-least-32-characters-long-for-security

# JWT 过期时间 (毫秒)
# 86400000 = 24小时
# 604800000 = 7天
JWT_EXPIRATION=86400000

# ================================================
# LLM API 配置
# ================================================

# --- OpenAI ---
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=sk-your-openai-api-key-here
LLM_MODEL=gpt-3.5-turbo

# --- 阿里通义千问 ---
# LLM_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
# LLM_API_KEY=sk-your-qwen-api-key-here
# LLM_MODEL=qwen-turbo

# --- 其他自定义代理 ---
# LLM_API_URL=http://your-custom-proxy.com/v1/chat/completions
# LLM_API_KEY=your-custom-api-key
# LLM_MODEL=your-model-name

# LLM 参数配置
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048
LLM_TIMEOUT=30

# ================================================
# 应用配置
# ================================================
APP_NAME=易经卜卦服务
APP_VERSION=1.0.0

# 是否启用认证 (true/false)
APP_AUTH_ENABLED=true

# 是否启用AI功能 (true/false)
APP_AI_ENABLED=false

# 每个用户每天最大卜卦次数
APP_MAX_DIVINATIONS_PER_DAY=10

# 管理员邮箱
APP_ADMIN_EMAIL=admin@example.com

# ================================================
# 文件存储配置
# ================================================
# 存储类型: local (本地) 或 s3 (云存储)
STORAGE_TYPE=local

# 本地存储配置
STORAGE_LOCAL_BASE_PATH=./uploads
STORAGE_LOCAL_BASE_URL=http://localhost:8080/uploads

# ================================================
# 通知配置
# ================================================
# 是否启用邮件通知
NOTIFICATION_EMAIL_ENABLED=false

# 发件人地址
NOTIFICATION_EMAIL_FROM=noreply@yy-yao.com

# 管理员通知邮箱
NOTIFICATION_ADMIN_EMAIL=admin@example.com

# ================================================
# 邮件服务器配置 (SMTP)
# ================================================
# 邮件服务器地址
MAIL_HOST=smtp.gmail.com

# 邮件服务器端口
MAIL_PORT=587

# 邮件账号
MAIL_USERNAME=your-email@gmail.com

# 邮件密码或应用专用密码
MAIL_PASSWORD=your-app-password

# Gmail 用户注意:
# 1. 启用两步验证
# 2. 生成应用专用密码 (https://myaccount.google.com/apppasswords)
# 3. 使用应用密码而非账户密码

# 其他常用 SMTP 配置:
# QQ 邮箱: smtp.qq.com:587
# 163 邮箱: smtp.163.com:25
# Outlook: smtp.office365.com:587
# 企业邮箱: 根据具体服务商配置

# ================================================
# 服务器配置
# ================================================
SERVER_PORT=8080

# ================================================
# 日志配置
# ================================================
# 日志级别: TRACE, DEBUG, INFO, WARN, ERROR
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_APP=INFO

# ================================================
# Actuator 配置
# ================================================
# 暴露的端点: health,info,metrics,env
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,metrics

# 健康检查详情显示策略: never, when-authorized, always
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=when-authorized
